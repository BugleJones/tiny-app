<!DOCTYPE html>

<html lang="en">
<head><title>TinyApp</title></head>
<%- include partials/_header %>
<body>
    <h1>TinyApp!</h1>
    <p>Origin: <%= longURL.longURL  %> TinyAppURL: <%= `http://localhost:8080/urls/${shortURL}` %></p>
    <form method="POST" action="/urls/<%= shortURL %>">
      <p>
        <label for="newLongUrl">New TinyAppURL:</label>
        <input id="longURL" type="text" name="longURL" value=<%= longURL.longURL %>>
      </p>
      <button type="submit">Update</button>
    </form>
</body>
</html>
